# @package _global_

defaults:
  - override /model: bert_tiny.yaml
  - override /datamodule: qqp.yaml

name: weak/tinybert/qqp
seed: 888
test_after_training: False

datamodule:
  batch_size: 32

model:
  learning_rate: 3e-5
  weight_decay: 0.1

trainer:
  max_epochs: 3
  gpus: 1
