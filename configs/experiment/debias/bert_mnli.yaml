# @package _global_

defaults:
  - override /model: bert_base_uncased.yaml
  - override /datamodule: mnli.yaml

name: debiasing/{type}/bert/mnli
seed: null

model:
  loss_fn: "reweight-by-teacher"
  use_teacher_probs: True

datamodule:
  bias_path: null

trainer:
  max_epochs: 3
  gpus: 1