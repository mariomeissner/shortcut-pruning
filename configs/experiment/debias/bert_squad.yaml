# @package _global_

defaults:
  - override /model: bert_squad.yaml
  - override /datamodule: squad.yaml

name: debiasing/{type}/bert/squad
seed: null

model:
  use_bias_probs: True
  # loss_fn: "reweight-by-teacher"

datamodule:
  bias_path: null

trainer:
  num_sanity_val_steps: 0
  max_epochs: 3
  gpus: 1
