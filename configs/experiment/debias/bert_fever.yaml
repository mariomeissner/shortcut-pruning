# @package _global_

defaults:
  - override /model: bert_base_uncased.yaml
  - override /datamodule: fever.yaml

name: null # Override me!! debiasing/{type}/bert/fever
seed: null

model:
  loss_fn: "reweight-by-teacher"
  use_bias_probs: True

datamodule:
  bias_path: null

trainer:
  max_epochs: 3
  gpus: 1
